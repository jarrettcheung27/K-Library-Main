Resoure for Custom agent and custom prompt: https://github.com/github/awesome-copilot
# Custom Agent
# Prompt Engineering
We can build a custom prompt in prompt file. Each time we want to reuse a prompt we can use the syntax: `/prompt_file_name xxx` (just like the `/explian` instruction)

# Model Selection
We can use the stronger (more expensive) model to make the plan and build the main body and use some cheaper model to implement the detail (minimal tasks).